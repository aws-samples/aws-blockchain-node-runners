##############################################################
# Base Sepolia Testnet - base-reth-node + base-consensus (Full Node)
##############################################################

# AWS Configuration
AWS_ACCOUNT_ID="xxxxxxxxxxx"
AWS_REGION="us-east-1"

# Availability Zone (optional - override automatic AZ selection for single-node deployments)
# AWS_AZ="us-east-1a"

# Protocol Configuration
BLOCKCHAIN_PROTOCOL="base"
DEPLOYMENT_MODE="single-node"
BC_NETWORK="base-sepolia"
CLIENT_CONFIG="base-reth-v1.2.0-full.yml"

# Instance Configuration
# i7i.12xlarge: 48 vCPU, 384 GB RAM, 4x 3750 GB NVMe (x86)
INSTANCE_TYPE="i7i.12xlarge"
CPU_TYPE="x86_64"

# Storage Configuration - NVMe instance store (all drives combined into RAID 0)
DATA_VOLUMES_COUNT="1"
DATA_VOL_1_TYPE="instance-store"
DATA_VOL_1_MOUNT_PATH="/data"
DATA_VOL_1_FILESYSTEM="ext4"

# Snapshot Configuration
# Reth snapshots significantly reduce initial sync time
# See https://docs.base.org/base-chain/node-operators/snapshots for details
SNAPSHOT_ENABLED="true"
SNAPSHOT_DOWNLOAD_URL="https://sepolia-reth-archive-snapshots.base.org"

# Traffic Shaping Configuration
TRAFFIC_SHAPING_ENABLED="true"
TRAFFIC_SHAPING_RATE_MBIT="40"
TRAFFIC_SHAPING_CHECK_INTERVAL_SEC="60"
TRAFFIC_SHAPING_MAX_BLOCKS_BEHIND="10"

# Base Custom Configuration
# L1 Sepolia RPC (required) - provide your own Ethereum Sepolia endpoint
BASE_L1_RPC_URL="https://your-ethereum-sepolia-rpc-url"
# L1 Sepolia Beacon API (required)
BASE_L1_BEACON_URL="https://your-ethereum-sepolia-beacon-url"
